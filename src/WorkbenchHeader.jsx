import { ControlWorkbenchHeader as FrameworkWorkbenchHeader } from "control-surface-ui/react";

export default function WorkbenchHeader(props) {
  return <FrameworkWorkbenchHeader headingLevel={2} {...props} />;
}
